[package]
name = "omniframe-core"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Native filesystem, atomic project persistence and job boundary for OmniFrame"

[lib]
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
