{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum capability set for the local OmniFrame editor window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
