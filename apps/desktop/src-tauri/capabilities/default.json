{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "No unrestricted native filesystem or shell capability is granted by default.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
