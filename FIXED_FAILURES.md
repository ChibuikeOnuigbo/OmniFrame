# OmniFrame Fixed Failures Registry (`FIXED_FAILURES.md`)

This register records permanent post-mortem entries for defects discovered, analyzed, and resolved across OmniFrame's architectural subsystems.

| Failure ID | Subsystem | Severity | Root Cause | Resolution | Verification Test |
|---|---|---|---|---|---|
| F-0001 | Timeline Data Model | High | Clip positions stored as relative coordinates prone to rounding drift during zoom. | Refactored data model to invariant temporal intervals: rational seconds (`start`, `duration`, `trackId`) converted to view pixels via dynamic `pixelsPerSecond`. | `qa/timeline-rebuild-e2e.mjs` |
| F-0002 | Track Hierarchy | High | Hardcoded single-track video container prevented NLE multi-track vertical stacking and picture-in-picture. | Implemented dynamic track model with track objects (`Track { id, name, kind, visible, locked }`), track insertion above/below, and track-aware spatial compositing. | `qa/timeline-rebuild-e2e.mjs` |
| F-0003 | Transitions | Critical | Transitions were simulated via ad-hoc clip CSS opacity classes rather than discrete objects. | Designed and implemented first-class `Transition` entity (`fromClipId`, `toClipId`, `type`, `startTime`, `duration`, `alignment`) with dedicated timeline rendering and trim handles. | `qa/timeline-rebuild-e2e.mjs` |
| F-0004 | Context Menus | Medium | Context menu events intercepted in capture phase defaulted unhandled elements to `EMPTY_EDITOR`. | Extended context menu resolver in `src/Studio.tsx` to detect `[data-testid="timeline-transition"]` and expose transition type presets and duration controls. | `qa/timeline-rebuild-e2e.mjs` |
| F-0005 | Export Pipeline | High | Export button lacked standard QA `data-testid="export-video-btn"` attribute, causing automated E2E selector timeout. | Added explicit `data-testid="export-video-btn"` to TopBar download button. | `qa/timeline-rebuild-e2e.mjs` |
| F-0006 | Automated Testing | High | Editor Zustand store unexposed on `window.__omniframe_store`, impeding automated assertion of transition state changes. | Exposed `(window as any).__omniframe_store = useEditor` in `src/store.ts`. | `qa/timeline-rebuild-e2e.mjs` |
| F-0007 | OpenCV Environment | Medium | Sandbox lacked `opencv-python-headless` package for video frame decoding during test execution. | Installed `opencv-python-headless` and `numpy` via pip with system package override. | `qa/timeline-rebuild-e2e.mjs` |
| F-0008 | Playwright Chromium | High | Sparticuz Chromium runtime missing uncompressed shared library binaries (`libnspr4.so`). | Added `await inflate(...)` and runtime `LD_LIBRARY_PATH` injection prior to browser launch. | `qa/timeline-rebuild-e2e.mjs` |
| F-0009 | Timeline Ruler | Medium | Ruler ticks overlapped and created visual noise at extreme zoom levels. | Implemented adaptive ruler interval algorithm switching between minute, second, half-second, and frame ticks. | `qa/timeline-ruler-e2e.mjs` |
| F-0010 | Media Library Ingestion | Medium | Audio files lacked duration extraction when metadata loading was delayed. | Implemented `AudioContext.decodeAudioData` fallback to reliably compute exact duration in seconds. | `qa/media-library-e2e.mjs` |
| F-0011 | Timeline Split Tool | High | Splitting at playhead failed when playhead was within 0.05s of clip boundary. | Added boundary guard tolerance preventing degenerate 0-second slices. | `qa/stress-e2e.mjs` |
| F-0012 | Right Panel Collapsibility | Low | Inspector panel toggle caused canvas reflow jump when snapping back into view. | Added CSS transition easing and `ResizeObserver` listener on preview canvas container. | `qa/stress-e2e.mjs` |
| F-0013 | Audio Waveform Caching | Medium | Re-generating peaks on every scrub caused noticeable UI thread stuttering. | Implemented persistent 256-point peak arrays in clip metadata, computed once on ingest. | `qa/audio-waveform-e2e.mjs` |
| F-0014 | Video Export Format | High | Canvas `MediaRecorder` crashed on unsupported codec combinations across different browser platforms. | Added MIME type negotiation falling back gracefully between `video/webm;codecs=vp9`, `video/webm;codecs=vp8`, and standard `video/webm`. | `src/lib/export.ts` |
| F-0015 | Settings Modal Dismissal | Low | Clicking inside settings popup backdrop triggered unintended background timeline seek. | Added backdrop `stopPropagation` and modal overlay capture shield. | `qa/reports/settings-popup-tests.md` |
| F-0016 | Transition Rendering in Export | High | Canvas export loop only drew individual clips without blending overlapping transitions. | Integrated shader/canvas transition blending loop into `src/lib/export.ts` rendering cross dissolves, dips, and wipes frame-by-frame. | `qa/timeline-rebuild-e2e.mjs` |
| F-0017 | Drawing UI Component Typing | Medium | `DrawingPanel` and `DrawingToolbar` tool icon typing declared as `React.FC<{ size?: number }>` conflicted with `LucideIcon` ForwardRef typing. | Updated tool definition type to `icon: LucideIcon` imported directly from `lucide-react`. | `qa/drawing-layout-e2e.mjs` |
| F-0018 | Left Dock Drawer Pointer Interception | Medium | Exiting focus mode left drawer collapsed, causing automated click on `add-paint-layer-btn` to be intercepted by adjacent preview viewport during CSS width animation. | Added explicit store drawer expansion (`setLeftOpen(true)`) and 400ms transition stabilization delay before pointer dispatch. | `qa/drawing-layout-e2e.mjs` |
| F-0019 | Layer Mask Conversion Fallback | High | `convertSelectionToMask` failed to generate mask data URL if `activeSelection.maskDataUrl` had not been pre-rendered during initial drag. | Integrated `createSelectionMask(sel, 1920, 1080)` inside `convertSelectionToMask` so any active rectangle, ellipse, or lasso selection immediately rasterizes into a crisp PNG mask before clearing selection. | `qa/layout-selection-mask-e2e.mjs` |
| F-0020 | Custom Workspace LocalStorage Key | Medium | Inconsistent localStorage key between test runner (`omniframe-custom-workspaces`) and store implementation (`omniframe.customWorkspaces`). | Unified key naming to `omniframe.customWorkspaces` with error handling and fallback parsing. | `qa/layout-selection-mask-e2e.mjs` |
| F-0021 | Layout Modal Tab TestIDs | Low | Test automation expected pluralized tab IDs (`tab-custom-workspaces`, `tab-focus-modes`) while modal used concise testids (`tab-custom`, `tab-focus`). | Updated E2E test selectors to target canonical modal tab testids. | `qa/layout-selection-mask-e2e.mjs` |
| F-0022 | Media Ingestion Decoupling | High | Ingesting media files automatically added clips to the timeline, violating the library/timeline separation invariant (Invariant I-10). | Decoupled `importFiles` in `src/store.ts` to add assets strictly to library and load into isolated `SourceMonitor` state. Added explicit "Add to Timeline" controls. | `qa/master-rebuild-e2e.mjs` |
| F-0023 | Same-Track Collision Overlap | Critical | Dragging or inserting clips on the same track allowed spatial overlaps without ripple shifting. | Implemented `resolveSameTrackRipple` algorithm in `src/store.ts` enforcing strict Invariant I-01 non-overlap with downstream ripple cascade. | `qa/master-rebuild-e2e.mjs` |
| F-0024 | Browser Drag Download Artifact | High | Internal clip and asset drag-and-drop operations triggered browser file save / `download.jpg` artifacts. | Implemented domain-typed drag data payloads (`application/x-omniframe-timeline-item`, `application/x-omniframe-asset`) and filtered internal drags from window drop targets. | `qa/master-rebuild-e2e.mjs` |
| F-0025 | Non-Target-Aware Context Menu | Medium | Right-clicking empty track lane displayed generic transition commands and lacked track-level gap operations. | Extended centralized context menu resolver in `src/Studio.tsx` to identify `TRACK_LANE`, exposing "Close Gaps on Track" and track management while blocking transition commands. | `qa/master-rebuild-e2e.mjs` |
| F-0026 | Aspect Ratio Export Synchronization | Critical | Selecting non-standard aspect ratios (e.g. 9:16 portrait) did not propagate to export dimensions, outputting mismatched video. | Synchronized `sequenceSettings` (`targetW`, `targetH`) with export engine and auto-switched monitor mode to `program` during recording. Verified via OpenCV. | `qa/master-rebuild-e2e.mjs` |
| F-0027 | UI Placeholder Stubs Elimination | High | Left dock panels contained planned/future prototype text stubs. | Replaced all stubs with functional components (`TransitionsPanel`, `EffectsPanel`, `TextPanel`, `ThreePanel`) and documented genuine platform boundaries in `UNIMPLEMENTED_INTERNAL.md`. | `qa/master-rebuild-e2e.mjs` |
| F-0028 | Voice Isolation Modal Initial Clip State Sync | High | Opening Voice Isolation Modal from context menu on newly imported clip failed because modal initial state was initialized once on component mount before clip ID was assigned. | Added `React.useEffect` prop synchronization and fallback selector resolving active clip, preventing unselected clip stalls. | `qa/voice-isolation-e2e.mjs` |
| F-0029 | Voice Isolation DSP Phase-Cancellation & Bass Retention | High | Center-channel vocal reduction without multi-band crossover caused bass drum and kick to be canceled along with dialogue. | Implemented Chamberlin SVF 3-band crossover splitting audio into sub-bass (<140Hz mono preservation), vocal formant band (140Hz-7500Hz mid-side phase cancellation), and air band (>7500Hz stereo ambience preservation). | `qa/voice-isolation-e2e.mjs` |
| F-0030 | Timeline Ruler Pointer Capture Swallowing Marker Double-Click | High | Double-clicking timeline marker flags on the timeline ruler failed to open the edit modal in test automation because ruler `onPointerDown` called `setPointerCapture(e.pointerId)`, redirecting all subsequent pointer events and clicks to the ruler scrub handler. | Added `onPointerDown={(e) => e.stopPropagation()}` to `[data-testid="timeline-marker"]`, preventing ruler scrub handler from capturing the pointer when interacting directly with sequence markers. | `qa/marker-audiometer-e2e.mjs` |
| F-0031 | Zero-Level Audio Meter Visibility in DOM-Based Verification | Medium | Playwright's `isVisible()` returned false on `[data-testid="vu-meter-left-bar"]` when no audio was actively playing because the inner LED meter element had `width: 0%`. | Updated test suite to verify DOM mounting and style attribute presence, while checking audio meter container visibility and master volume slider adjustments. | `qa/marker-audiometer-e2e.mjs` |
| F-0032 | Text Clip Canvas Playback Freeze & Missing Render Branch | Critical | Playback engine looked up `st.assets.find(a => a.id === clip.assetId)` for text clips, returning undefined because text clips use synthetic asset IDs (`text-asset`), flagging `visualPending = true` and skipping frame rendering entirely. | Added dedicated `drawTextClip(ctx, clip, canvasW, canvasH)` routine in `src/lib/playback.ts` with custom font family, size scaling, drop shadows, and pill backgrounds, bypassing asset element lookup. | `qa/unified-preview-text-effects-mobile-e2e.mjs` |
| F-0033 | Transition Duration Overshoot Aborting Video Blending | High | During transition crossfade, computing `target` time relative to incoming clip in `syncElement` could produce negative offsets or values exceeding duration, returning null and causing transition rendering to abort. | Added strict mathematical clamping `Math.max(0, Math.min(srcDur, target))` in `syncElement` and fallback video/canvas rendering in `drawTransition`. | `qa/unified-preview-text-effects-mobile-e2e.mjs` |
| F-0034 | Dual Monitor Program vs Source Cognitive Split | High | Separate Program and Source monitor tab pills caused confusion, requiring manual tab switching to preview media assets and leaving preview state decoupled from timeline. | Unified preview into a single viewport: eliminated Program/Source tabs, integrated direct asset preview with transport controls, seamless one-click return to timeline sequence, and interactive 3D Orbit Viewer canvas. | `qa/unified-preview-text-effects-mobile-e2e.mjs` |
| F-0035 | Mobile Viewport LeftDock Layout Overflow | High | LeftDock drawer absolute positioning on desktop caused pointer event interception with preview canvas, while flex layout on mobile caused horizontal document scroll overflow (`scrollWidth > innerWidth`). | Scoped absolute drawer styling strictly to mobile (`max-sm:absolute max-sm:left-12 max-sm:top-0 max-sm:bottom-0 max-sm:z-40 max-sm:shadow-2xl`) while keeping clean flex child on desktop, verifying 0px overflow on 390px viewport. | `qa/unified-preview-text-effects-mobile-e2e.mjs` |
| F-0036 | Ambiguity Between Isolate Voice and Separate Audio | Medium | Timeline context menu exposed separate voice isolation actions indiscriminately alongside demuxing ("Separate audio"), causing user confusion. | Standardized timeline context menu to display a single "Isolate Voice" parent item with a hover side-flyout submenu containing "Remove Vocal" and "Keep Vocal", keeping "Separate audio" strictly for video track demuxing. | `qa/unified-preview-text-effects-mobile-e2e.mjs` |
| F-0037 | Canvas Empty Notice Visible During Preview Playback | Medium | Overlay message 'Your canvas is empty' remained visible during playback when playhead moved or audio/engine was running without static timeline clips. | Updated conditional guard in `src/components/Preview.tsx` to hide notice whenever `playing`, `assetPlaying`, `clipCount > 0`, `isPreviewingAsset`, `is3DMode`, or `drawingEnabled` is active. | `qa/ratio-dropdown-canvas-empty-e2e.mjs` |
| F-0038 | Aspect Ratio Dropdown Generic Icons | Low | Aspect ratio trigger and dropdown used generic TV and smartphone icons without real platform identity or 9:16 stacked card visualization. | Implemented custom SVG platform icons (YouTube, Instagram, Cinemascope, Custom) and designed `YouTubeTikTokPaperSlideIcon` showing overlapping YouTube and TikTok cards slid to the side like a paper deck. | `qa/ratio-dropdown-canvas-empty-e2e.mjs` |
| F-0039 | Hyphenated Words in User-Facing Text Throughout | Low | Multiple user-facing UI labels, descriptions, and settings used hyphens (e.g. 'Full-screen', 'Ultra-Wide', 'real-time', 'Drop-frame', 'Invert / X-Ray'). | Removed all hyphens from user-facing text strings throughout the application ('Fullscreen', 'Ultra Wide', 'realtime', 'Drop frame', 'Invert / X Ray'). | `qa/ratio-dropdown-canvas-empty-e2e.mjs` |
| F-0040 | Optical Flow & Sobel Multi-Signal Tracking Session Integration | High | Tracking system lacked multi-signal gradient magnitude and forward-backward consistency checks, risking spatial drift across fast limb motion. | Implemented LumaCut tracking engine (`src/lib/trackingEngine.ts`) with Sobel horizontal/vertical filters, gradient magnitude penalty, NCC patch matching, and bidirectional verification. Added `TrackingPanel.tsx` with dual tracking modes (Mask Tracking vs Main Tracking). | `qa/supreme-master-command-e2e.mjs` |
| F-0041 | Universal LinkSet Member Ripple and Atomic Cascading Deletion | High | Moving or deleting one member of a linked set failed to move or delete associated elements in an atomic undoable transaction. | Implemented Universal LinkSet model in `src/store.ts` supporting boolean capability rules (`motion`, `duration`, `delete`, `selection`). Updated `moveClip` and `removeClip` to cascade changes atomically across all linked members. | `qa/supreme-master-command-e2e.mjs` |
| F-0042 | AI Background Matting Hardware Acceleration Fallback | High | AI background segmentation assumed WebGPU without graceful fallback to WebAssembly SIMD or WebGL2, causing crashes on unsupported environments. | Implemented `detectHardwareAcceleration` and multi-model matting architecture (`src/lib/bgRemovalEngine.ts`) supporting BiRefNet, MODNet, ISNet, and SlimSAM with automatic backend detection, edge choke/expand, and temporal smoothing. | `qa/supreme-master-command-e2e.mjs` |
| F-0043 | 3D Shared Texture "Make Unique" Data-Block Branching | Medium | Editing a shared material texture inadvertently mutated all other 3D geometry sharing the same texture block. | Added Blender-style users count tracking to `TextureAsset` and implemented `makeTextureUnique` in `src/store.ts` and `ThreePanel.tsx`, deep-cloning shared texture assets into independent instances. | `qa/supreme-master-command-e2e.mjs` |
| F-0044 | Masking Subsystem Conflated with Drawing Tools | High | Spatial masking and selection marquee tools were placed in the drawing & paint toolbar, creating user confusion and disconnecting masks from temporal tracking. | Decoupled Masking entirely from Drawing: moved spatial mask creation (Box, Circle, Polygon, Brush), inversion, feathering, and expansion into `TrackingPanel.tsx` under Mask Track mode, directly linking masks to optical flow tracking and deformation. | `qa/masking-tracking-mobile-drawing-e2e.mjs` |
| F-0045 | Floating Drawing Toolbar Width Exceeding Mobile Phone Screens | High | The horizontal drawing toolbar stretched to over 800px with 16 buttons and lengthy tooltip titles (e.g. `Clone Stamp (Alt+Click to set source)`), overflowing mobile phone screens (390px). | Made `DrawingToolbar` strictly responsive with `max-w-[calc(100vw-16px)]`, `overflow-x-auto scrollbar-none`, compact 28px buttons on mobile, and shortened all tool titles to concise single words (<=14 chars). Verified 374px width <= 390px on mobile with zero page overflow. | `qa/masking-tracking-mobile-drawing-e2e.mjs` |
| F-0046 | Obstructive Top Timeline Sequence Banner & Canvas Clutter | Medium | A large floating `Timeline Sequence` pill rendered at the top-left of the preview stage, colliding with the top controls and obscuring buttons on mobile screens. | Removed the bulky top banner entirely; replaced it with a tiny, short ping icon (`[ • Seq ]`) positioned down close to the timeline (`bottom-2 left-2`) beside the aspect ratio trigger. Repositioned the drawing toolbar to float neatly above the bottom controls, freeing the entire video stage. | `qa/masking-tracking-mobile-drawing-e2e.mjs` |
| F-0047 | Generic Rotation Icon in Clip Inspector and 3D Viewport | Medium | Clip inspector and 3D orbit guide lacked 3-axis rotation visualization, using generic 2D arrows or plain text. | Created authentic Blender 3-axis rotation gizmo (`src/components/icons/BlenderRotationIcon.tsx`) with RGB rotation rings (X=Red, Y=Green, Z=Blue), directional arrowheads, outer trackball, and central pivot. Integrated in `RightPanel.tsx` and `ThreePanel.tsx`. | `qa/blender-rotation-e2e.mjs` |
| F-0048 | Verbose Explanatory Essays Across Studio Dialogs and Panels | Low | Dialogs and panels in Voice Isolation, Tracking, LinkPanel, ThreePanel, and Settings contained redundant multi-line explanations for self-evident audio/video editing terms. | Performed complete text audit, eliminating wordy essays and preserving only concise 1-sentence descriptions for unique concepts. Reduced visual clutter and improved UI scanning speed. | `qa/voice-isolation-e2e.mjs` |
| F-0049 | Video Thumbnail Squashing & Aspect Ratio Distortion | High | Canvas thumbnail generation rendered raw video frames directly into fixed 16:9 canvas dimensions without aspect ratio calculations, distorting portrait and non-standard video assets in the media panel. | Implemented aspect-ratio-preserving letterbox calculation (`contain`) in `captureVideoThumbnail` (`src/store.ts`) and CSS `object-contain bg-ink-950` in `MediaPanel.tsx`. | `qa/source-preview-oop-tracks-e2e.mjs` |
| F-0050 | Timeline Insertion Appending to End Instead of Current Playhead | High | Adding assets (text, media) inserted clips at the timeline end or 0.0s rather than at the active scrubber playhead position, disorienting editing workflows. | Updated `addClipToTrack` and `addTextTitleClip` in `src/store.ts` to insert at `atTime ?? get().playhead` and dynamically overlay text onto a secondary video track if a clip already occupies the playhead. | `qa/source-preview-oop-tracks-e2e.mjs` |
| F-0051 | Redundant '+ Add to Timeline' Button in Source Preview Banner | Medium | Source preview file banner included an extraneous '+ Add to Timeline' button that cluttered the preview stage. | Relocated file details banner to bottom-left pill in `src/components/Preview.tsx` with clean format badge and close button, removing the redundant button. | `qa/source-preview-oop-tracks-e2e.mjs` |
| F-0052 | Narrow Source Transport Bar with Small Touch Targets | Medium | Source preview controls were restricted to a narrow pill, making scrubbing and transport controls difficult to use accurately. | Expanded source transport bar to full preview width with increased height (44px) and comfortable touch/click targets in `src/components/Preview.tsx`. | `qa/source-preview-oop-tracks-e2e.mjs` |
| F-0053 | Text-Only Timeline Tracks Consuming Full Video Track Height (64px) | High | Timeline tracks rendered at a uniform fixed height regardless of media content, wasting vertical screen space on title and caption overlays. | Built OOP `TrackModel` and `TimelineController` (`src/lib/oop/TimelineController.ts`) implementing CapCut-style responsive track heights: text-only tracks reduced by 40% (38px), expanding to standard height (64px) if video clips are present. Text clips styled with amber palette and font previews. | `qa/source-preview-oop-tracks-e2e.mjs` |
| F-0054 | Track Header Sidebar Layering Below Playhead Marker Line | Medium | Timeline header sidebar had z-index equal to or below the red playhead marker line, causing the playhead line to overlap the sticky track headers during horizontal scrolling. | Elevated sticky track header column to `z-40` in `src/components/Timeline.tsx`, strictly above the playhead marker (`z-25`). | `qa/source-preview-oop-tracks-e2e.mjs` |
| F-0055 | Video Playback Halted Upon Entering 3D Viewport | High | `Preview.tsx` disposed `PreviewEngine` whenever `is3DMode` became true, halting sequence playback and unmounting the canvas stage so video could not play in 3D. | Kept `PreviewEngine` running continuously during 3D mode, retained `<canvas id="of-canvas">` mounted with `opacity-0 pointer-events-none`, and piped frames directly into `THREE.CanvasTexture(sourceCanvas)` with `needsUpdate = true` on every animation tick. | `qa/three-d-video-plane-e2e.mjs` |
| F-0056 | Inability to Aim Camera at Video vs 3D Objects in "3D in 2D" Compositing | High | `ThreeViewer.tsx` replaced the video plane whenever a 3D primitive was chosen and lacked target-aware camera kinematics to frame either the video or 3D objects. | Co-located the 2.5D Video Plane and floating 3D objects in the same scene graph; implemented Camera Aim buttons (`aim-video-btn`, `aim-object-btn`, `aim-composite-btn`) dynamically adjusting spherical target coordinates and dollying camera. | `qa/three-d-video-plane-e2e.mjs` |
| F-0057 | Context Menu Stuck on Non-Menu Element Clicks and Touches | Critical | `pointerdown` dismissal listener on `window` operated in bubbling phase, so any child element with `stopPropagation()` (canvas, clips, docks) prevented the close handler from executing, leaving the context menu permanently stuck on screen. | Attached `pointerdown`, `touchstart`, `mousedown`, and `click` listeners to `window` with capture phase `{ capture: true }`, intercepting outside clicks before any child element can swallow the event. | `qa/audio-section-context-dismiss-e2e.mjs` |
| F-0058 | Intrusive Popup Modals Launched from Context Menu Voice Isolation | High | Context menu submenu included a modal settings launcher item that broke fast inline editing workflows and contradicted user intent for direct context commands. | Streamlined context menu submenu to strictly two direct actions (`Remove Vocal (Instrumental)` and `Keep Vocal (Dialogue Only)`), executing stem extraction instantly and closing the menu with zero popups. | `qa/audio-section-context-dismiss-e2e.mjs` |
| F-0059 | Clunky Dedicated Voice Isolation Panels Disconnected from Clip Inspection | High | Audio isolation was separated into a standalone panel with lengthy text, missing from the clip inspector where users actually configure audio properties. | Replaced standalone isolation layout with CapCut-style `[ ] Voice Isolation` checkbox in the Inspector's `Audio` section, which dynamically reveals mode selection (`Remove Vocal` / `Keep Vocal`) and the `omni-voicetarget` neural model selector. | `qa/audio-section-context-dismiss-e2e.mjs` |





