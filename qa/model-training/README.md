# Model-training evidence

This directory is reserved for measured training artifacts. `modeltrainer.py` may generate event logs, checkpoints, `report.json`, `report.html` and `report.svg` here; generated files are ignored so a long run does not flood Git. The report is safe to inspect locally and records validation metrics, example predictions and checkpoint/pass counts.