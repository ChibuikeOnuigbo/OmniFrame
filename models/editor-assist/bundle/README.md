# OmniFrame Editor Assist

This bundle contains an explicitly trained hashed-feature action model. Load `manifest.json` before using `model.onnx`; the tokenizer and feature count must match.

The model is optional and never required for editing, masking, tracking or export.
